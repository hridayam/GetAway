{
  "name": "getaway-project",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "install-client": "cd getaway && npm install",
    "install-server": "cd server && npm install",
    "client": "npm start --prefix getaway",
    "server": "npm start --prefix server",
    "initial-start": "npm install && npm run install-client && npm run install-server && npm run start",
    "start": "concurrently \"npm run client\" \"node server/app.js\""
  },
  "author": "",
  "license": "ISC",
  "private": true,
  "dependencies": {
    "bootstrap": "^4.1.3",
    "concurrently": "*",
    "react": "^16.5.0",
    "react-bootstrap": "^0.32.4",
    "react-dom": "^16.5.0",
    "reactstrap": "^6.4.0"
  }
}
