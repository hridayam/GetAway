{
  "name": "getaway-project",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "install-client": "cd getaway-client && npm install",
    "install-server": "cd server && npm install",
    "client": "npm start --prefix getaway-client",
    "server": "npm start --prefix server",
    "initial-start": "npm install && npm run install-client && npm run install-server && npm run start",
    "start": "concurrently \"npm run client\" \"node server/app.js\""
  },
  "author": "",
  "license": "ISC",
  "private": true,
  "dependencies": {
    "concurrently": "*"
  }
}
